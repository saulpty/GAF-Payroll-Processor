# PTO Payroll Evidence + Cleanups

[x] 1. loadPtoEmployeeDetail.ts — add payroll sub-select to ledger + pending
[x] 2. PtoSubRow.tsx — add payroll field to SubItem, new "In payroll" column
[x] 3. PtoBreakdown.tsx — drop summary line, add Retry button, pass payroll field
[x] 4. PtoTable.tsx — remove search, replace count caption with onCountsChange prop
[x] 5. PtoTracker.tsx — receive counts, show caption left of As-of
