I ran the query successfully from Monday's own API Playground and have the full column list. The temporary diagnostic file is no longer needed.

Delete `src/actions/debugMondayColumns.ts`. That is the only change. Do not modify, create or rename any other file. Nothing imports it, so nothing else needs updating.

Acceptance: `src/actions/debugMondayColumns.ts` no longer exists, and the project is otherwise byte-identical to its current state.
