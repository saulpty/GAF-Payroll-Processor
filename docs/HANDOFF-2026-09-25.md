# Handoff 2026-09-25: payroll run with Tim, Jeanine, design system, Action Required fixes

**Live in production: Hub 8.14.0** (released 2:33 PM by Claude on Saul's instruction).
Git `main` = `0ac9c03`, tests **585/585**.

## What shipped today

| Area | What changed | Release |
|---|---|---|
| Jeanine Puyol | Duplicate employee (id 62) merged into id 20; email now `jeanine.p@passiontocarehc.com`. Migration `1782014000`. | data, 1:22 PM |
| Roster Save | Editing a person updates them by id; an email already used by someone else is refused with a message. Was: new email → a second employee. | 8.13.0 |
| **Payroll Master "Save crashes"** (Tim) | After every save the page tore down and rebuilt the whole table (444 rows, 28k dropdown options) and froze the tab for over a minute. Now the table stays on screen, dims while refreshing, save done in ~5 s. | 8.14.0 |
| Action Required | No flash on commit; committed rows leave at once; toast `Committed N rows to green · Undo`; red/yellow counts and the nav badge refresh after commits; impossible times ("55:00 PM") and an impact without an event are refused with a reason; red pulsing Event box `Pick an event first`; bulk bar only for 2+ rows, one row has its own `Commit` button; editing no longer selects a row. | 8.14.0 |
| Payroll data bug (pre-existing) | A bulk edit could copy one employee's punch times onto another selected row hidden by a filter. Fixed: bulk edits only reach visible rows, each from its own data. | 8.14.0 |
| Time box | Impossible times turn the box red (`Not a real time`); `1230a` now gives 12:30 AM (was "0:30 AM"; same minutes); stored "0:30 AM" reads as 12:30 AM. | 8.14.0 |
| Design system | Warm variant + Title Case chosen. Rules in `docs/DESIGN-SYSTEM.md` (full) and `src/DESIGN.md` (read by UIB's AI). Warm tokens, animations, `fmtTime`/`fmtShift`, `ds/Combobox`, `ds/Toast`, `ds/BulkBar` added. **Visible only on redesigned pages** (page-by-page rollout). Preview: https://claude.ai/artifact/2PwGWuCuVqL3C6LZaYHi54 | 8.14.0 (no visual change yet) |

## For Tim (tell him before he commits again)
- Reload the app once (Ctrl+Shift+R) to get 8.14.0.
- Action Required: one row → the small **Commit** button next to the name. The blue bar is for
  2+ rows. Editing a row no longer ticks it.
- If a row is refused, the dialog/toast says why. An impact needs its event first.
- **Behaviour change:** a row with an impact but no event is no longer saved at all (before, it
  was written but stayed not-ready).
- Undo = the same as the Committed list's revert: the row goes back to Action Required with its
  chosen events; times edited before the commit stay edited.

## Not verified live
The Action Required **commit itself** was not clicked on a real row: every candidate was a live
pending row whose save would recompute its discount. The save code path is unchanged (same
actions, same values), the new wrapper was code-reviewed, and static tests guard it. **Watch the
first real commit** (toast appears, row leaves, counts drop).

## Evening batch (after 8.14.0): all pendings

| Area | What changed |
|---|---|
| Short names | **Gisselle Ramos** (#22), **Maria Urriola** (#52). Old full names kept as aliases, so Monday name matching and Disciplinary records still find them. Migration `1782015000`. |
| Process Payroll warning | "Teramind covers only X/10" is now "no Teramind data and no form on N of M workdays: Wed Sep 16, …". Days explained by a Monday absence form, a permission or a holiday no longer count. Warning text only. |
| Action Required, Warm look (AR-4) | Title Case headers; dates `Fri Jun 5`; **Shift** `Mon–Fri · 9AM–5PM`; minutes `4h 11m`; live **Discount** column (same math as the save); searchable dropdowns with ×; orange selection bar; floating dark bulk bar; sort by Event 1 / Impact 1. |
| Filters (AR-5) | **Event** dropdown, **Needs an Event N** chip, `Showing X of Y`. |
| Committed section (AR-6) | Same look; `Wed Sep 23`; `Fri Sep 25 · 6:58PM`. |
| Undo (AR-7) | The toast's Undo now restores the row exactly as it was (times included). The Committed list's revert button is unchanged. |
| Roster | Split for size (15,000 → 10,243 bytes); no visible change. |

## Still to do
All of Saul's notes from the payroll run are done. Remaining, by choice:
1. **Payroll Master split** (46 KB, limit 15 KB). A deliberate project of its own, not an
   end-of-day change: it is the busiest payroll page. Same method as Action Required
   (pure move first, then changes). It should also get the Warm look afterwards.
2. Code review #13: overlapping reloads (Undo while a commit's reload is still running) can
   briefly show a stale list; the next refresh corrects it.
3. `ArBits.tsx` (`BroadcastSelect`, `Th`, `SortIcon`) is no longer used after AR-4; delete it
   in the next Action Required change.
4. The live Action Required **commit** has not been exercised on a real row by Claude (only the
   guards and UI). Watch Tim's first commit.

## Lessons from today (also in LESSONS.md)
- **UIB's AI can "compact its context" mid-prompt and then improvise.** AR-2 (21 KB) did: it
  edited forbidden `app.tsx` and hand-patched the page. Recovery: smaller prompts, each opening
  with a **copy-exactly** rule, and compatible-first ordering (new files first, switch-over last).
  Every later prompt landed byte-identical.
- **The Windows clipboard can be locked** by another app. Workaround used: a tiny local CORS file
  server (`127.0.0.1:8765`) and `fetch()` from the builder tab into the textarea.
- **"too many connections for role"** runtime banners are transient connection pressure: Ignore,
  never Fix.
- The draft is the whole app: releasing the Roster fix first kept it from shipping tangled with
  the design work.
