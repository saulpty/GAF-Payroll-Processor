# Handoff — 2026-09-10

Round 2 of the PTO tracker, from Saul's first pass over round 1. Built on
`/dev`, merged to `main`. **Saul released 6.9.0 to prod himself at 11:23 AM**
(the UIB release dialog showed "Released successfully" while prompt 16c was
finishing); 16c — the Status column moving before the buttons — landed on
`/dev` right after, so prod is one cosmetic step behind `/dev` until the next
release. Prompts:
`docs/superpowers/prompts/2026-09-10-pto-review/`.

## What changed

1. **Pending → Review.** The column, the header count and the "Only with
   review" filter count only requests that can be recorded *now*: return date
   passed **and** payroll processed through it. Everything else shows as a
   muted "N not yet" beside the chip. Requests whose return is before their
   leave are never counted.
2. **Nav badge** on the PTO Tracker tab with the global review count; it
   refreshes when a request is recorded, restored or withdrawn (new
   `ptoVersion` signal in `GlobalFilterContext`, which also fixes the stale
   `periodsVersion` memo dependency).
3. **Pay impact next to every event** in the In-payroll cell and the dialog
   panel: `Ausencia Justificada. ×1 · Incapacidad`. Same event with two
   impacts stays two lines.
4. **Record waits for payroll.** `recordability()` in `ptoPayrollMatch.ts` is
   the one rule: invalid dates → future → payroll not processed → ok. The row
   button, the record-mode dialog and the SQL counts all use it. Edit and Add
   manually keep only the "return date has passed" rule so pre-March rows stay
   editable.
5. **Broken requests are flagged in the row**: red chip *Return is before
   leave — fix on Monday*, Record disabled with *dates don't make sense*.
6. **Refresh in place.** Withdraw / Restore / Record / Edit no longer unmount
   the table; the expanded employee and the scroll position survive.
7. **Rows read as a sentence.** After two mockups the breakdown is
   `Type · Requested · Verdict · Payroll · actions`: the verdict is one short
   bold line in plain words (*Was out 7 days, not 5* / *back Mon Aug 24*;
   *Payroll has this as Permiso Remunerado* / *not a floating holiday*;
   *Matches*; *Payroll hasn't run for these days yet*), the Payroll column is
   the evidence (cycle, `Event ×N · Impact` lines). `PtoVerdictCell.tsx` holds
   the eleven state → sentence rules; `PtoPayrollCell.tsx` is evidence only.
   Polish pass (prompt 16, from Saul's "what's wrong here"): the table no
   longer stretches so the buttons sit beside the evidence; the type is plain
   text and the **status chip is its own column, between Evidence and the buttons**; the source reads `from
   Monday board` / `from Excel` / `added manually` (and nothing for a
   Monday-recorded row — "Monday" next to "Mon" read as a weekday); one fact
   is stated once (future rows: `in 88 days`, no line under the button);
   real plurals; Withdraw is a quiet ghost button; and a `Matches` row
   shows `also Permiso Remunerado · Incapacidad` when payroll has a second
   event inside the window, so "Matches" never hides one.
8. **Period-name guard** (Saul's explicit OK to touch `ProcessPayroll.tsx`):
   the name is trimmed, must look like `Q1-Aug-2026`, and a near-miss of an
   existing period is refused with the look-alike named. Plus a `NOT VALID`
   CHECK on `periods.period_name` so a bad name fails even if the UI is
   bypassed; the two legacy free-text names stay.

## The duplicate August period

`docs/findings/2026-09-10-q1-aug-duplicate-period.md`. Short version:
`Q1-Aug-2026` was an orphaned partial run (205 rows, 21 people, stops Aug 7, no
`periods` row); `Q1-Aug-20260` the complete run (437 rows, 43 people). Every
orphan row also existed in the complete run. **Fixed on Saul's go** (prompt 12,
migration `1782000100`, 15:53): the 205 orphan rows and 3 snapshots deleted,
`20260` renamed to `2026` in `periods`, `payroll_entries`, `run_snapshots`,
`hrk_exports`. Verified: one `Q1-Aug-2026` with 437 rows / 43 people; the
period dropdown shows `Q1-Aug-2026` and no `20260`; Gabriel Chu's Jul 29 row
lists one cycle.

**Process Payroll now pre-fills the next period** (`nextPeriod` in
`periodName.ts`, prompt 14): Q1 → Q2 of the same month, Q2 → Q1 of the next;
start = the day after the latest end, end = start + 14. Today it offers
`Q1-Sep-2026`, Aug 25 → Sep 8. Tim can overwrite it; the guard still checks
what he types.

## Tests

**245 passing** (232 → 245): M21–M25 in `ptoPayrollMatch.test.ts`, P1–P5 in
`periodName.test.ts` (P6–P8 cover `nextPeriod`). `RecordApprovalDialog.tsx` is 13.5 KB after the split
(was 14.1); `TopNav.tsx` 12.6 KB.

## Coverage, precisely (prompt 08b)

"Payroll processed" means a processed cycle **contains the leave date** and
payroll runs through the return date. A request that ends before payroll
history began (2026-03-11) is `before_history`: nothing will ever cover it,
so it is recordable on the Monday request alone and the cell says so. A gap
between cycles is *not* covered. Live counts after this: **24 to review**
(was 33 by the return-date rule alone).

## Things that went wrong on the way

- My first `nearMatch` returned the *first* candidate within one edit, so
  `q1-aug-2026` resolved to `Q2-Aug-2026`. Test P3 caught it; candidates are
  ranked now (same letters beat prefix beat one edit).
- Prompt 07's coverage rule counted February requests as covered because
  payroll "ran past" February. Caught by reading the live count (24 vs the
  33 I had reported) before Saul did; fixed in 08b with a test that pins the
  gap case.
- The period-name guard cannot be exercised in the browser without a
  Teramind file (the required-fields check runs first). Verified by reading
  the diff: one import, two checks, six `.trim()`s, nothing else.
- The Chrome extension's screenshot got stuck at a 184×92 clip again; every
  export this session went through the DOM route in LESSONS.
- Prompt 14 added a `useEffect` without its import; `/process` crashed with
  "Something went wrong". Caught by loading the page, fixed by 14b. LESSONS
  updated: the "confirm every identifier is imported" line belongs on prompts
  that *add* code too.
- Right after the two migrations ran, every PTO breakdown failed with the same
  "Unknown error" as yesterday, deterministic on Retry for a few minutes, then
  worked with no change to any file. The action was unchanged since it last
  worked. Treat an "Unknown error" in the minutes after an auto-applied
  migration as the datasource settling, and re-check before diagnosing.

## Open

- Prompt 12 (Aug data fix) awaits Saul's go.
- Reggina Sandoval's recorded PTO for Dec 31 → Jan 4, 2027 (recorded before
  the guard) — Saul said done.
- Six floating holidays on Mon Feb 16 (Carnival): February payroll is not in
  the system, they will stay "after payroll runs" forever unless Feb is
  processed or they are added manually.
